/**
 * Release Management Setup - Layout Route
 * Parent route for setup landing and flow
 */

import { Outlet } from '@remix-run/react';

export default function SetupLayout() {
  return <Outlet />;
}
